# RemoteExcavator 系统架构文档

## 1. 系统概述

RemoteExcavator 是一个基于 Isaac Sim 仿真的远程遥控挖掘机系统，通过 WebRTC 技术实现低延迟的视频传输和实时控制。系统采用分布式架构，支持从远程位置对仿真挖掘机进行实时操控和监控。

### 1.1 系统特点

- **超低延迟通信**: 采用 WebRTC 技术，端到端延迟约 20ms
- **硬件加速编码**: 支持 NVIDIA GPU 硬件编码（nvh264enc）
- **实时双向控制**: 支持视频流上行和控制指令下行
- **进程隔离架构**: 通过共享内存实现进程间通信，避免库冲突
- **分布式部署**: 支持云服务器部署，实现远程访问

### 1.2 技术栈

- **仿真平台**: Isaac Sim 5.1.0
- **通信框架**: ROS2 Humble
- **实时通信**: WebRTC (Pion)
- **视频编码**: GStreamer (硬件加速)
- **编程语言**: Python 3.10+, Go 1.18+
- **操作系统**: Ubuntu 22.04

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          软件/系统层                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌──────────────────────┐  ┌──────────────────────┐  ┌─────────────────┐ │
│  │  挖掘机仿真          │  │  挖掘机侧控制软件     │  │  云服务器端软件  │ │
│  │  (Simulator)         │  │  (Excavator-side)    │  │  (Cloud Server) │ │
│  │                      │  │                      │  │                 │ │
│  │  - 数字化传感器      │  │  - 图像处理软件       │  │  - 信令服务器   │ │
│  │  - 挖掘机本体        │  │  - 控制软件          │  │  - 管理系统     │ │
│  │  - 挖掘机场景        │  │  - 实时通信客户端    │  │                 │ │
│  └──────────────────────┘  └──────────────────────┘  └─────────────────┘ │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                    远程遥控侧软硬件                                    │ │
│  │  (Remote Teleoperation-side)                                        │ │
│  │  - 数据展示硬件系统                                                  │ │
│  │  - 遥控驾驶硬件系统                                                  │ │
│  │  - 遥控驾驶辅助系统                                                  │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                          硬件层                                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐           │
│  │  仿真电脑     │◄────►│ Orin域控制器  │◄────►│  云服务器     │           │
│  │              │网线  │              │       │              │           │
│  └──────────────┘      └──────────────┘      └──────────────┘           │
│                                                      │                   │
│                                                      │                   │
│                                            ┌─────────▼─────────┐         │
│                                            │  遥控侧硬件        │         │
│                                            │ (摇杆、踏板、显示器)│         │
│                                            └───────────────────┘         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 数据流架构

**数据方向说明**：
- **上行数据**：视频流上传（从挖掘机端到远程控制端）
- **下行数据**：控制指令下发（从远程控制端到挖掘机端）

#### 2.2.1 视频流（上行：挖掘机 → 控制端）

```
Isaac Sim 仿真环境
    ↓
挖掘机数字化传感器
    ├─ /front_camera (鱼眼相机)
    ├─ /front_surround_camera (环视相机)
    ├─ /left_camera (鱼眼相机)
    ├─ /rear_camera (鱼眼相机)
    └─ /right_camera (鱼眼相机)
    ↓
图像处理软件 (ImgProvider)
    ├─ multi_camera_stitcher_node (多摄像头拼接)
    │   └─ 或 pip_camera_stitcher_node (画中画拼接)
    └─ /stitched_image (拼接后的图像, rgb8)
    ↓
ROS2 到共享内存桥接 (ros_to_shm.py)
    └─ /dev/shm/isaac_rgb_buffer (共享内存)
    ↓
GStreamer 硬件编码器 (shm_to_stdout.py)
    └─ H.264 编码流 (stdout)
    ↓
Go WebRTC 客户端 (main.go)
    └─ WebRTC 视频轨道
    ↓
云服务器信令服务器 (Signaling Server)
    ↓
远程控制端浏览器 (exca-teleop)
    └─ 视频显示 (HTML5 video 元素)
```

#### 2.2.2 控制流（下行：控制端 → 挖掘机）

```
远程控制端浏览器 (exca-teleop)
    └─ 控制指令 (摇杆/键盘输入)
    ↓
WebRTC DataChannel 
    ↓
云服务器信令服务器 (Signaling Server)
    ↓
Go WebRTC 客户端 (main.go)
    └─ 接收控制指令
    ↓
ROS2 控制转发器 (ros_control_stdin.py)
    └─ /controls/teleop (ROS2 话题)
    ↓
控制软件 (teleop_prismatic_publisher.py)
    └─ /pc2000_joint_command (JointState)
    ↓
Isaac Sim 挖掘机本体
    ├─ 铲斗控制 (bucket_linear)
    ├─ 斗杆控制 (arm_linear)
    ├─ 动臂控制 (boom_linear)
    ├─ 回转控制 (body_rotate)
    └─ 履带控制 (track velocities)
```

---

## 3. 核心组件详解

### 3.1 挖掘机仿真 (Simulator)

**位置**: `simulator/`

**功能**:
- 提供 Isaac Sim 仿真环境
- 模拟挖掘机本体和场景
- 生成传感器数据（相机、雷达）

**主要组件**:

#### 3.1.1 传感器模拟 (`sensor_simulation/`)
- **鱼眼相机**: 4 个鱼眼相机（前、左、后、右）
- **环视相机**: 1 个环视相机
- **雷达**: 支持 Livox 和 Robosense 雷达模拟（支持自定义配置）

**配置文件**:
- `data/lidar_configs/Livox/` - Livox 雷达配置（Avia, Mid360, Mid40）
- `data/lidar_configs/Robosense/` - Robosense 雷达配置（Airy）
- 配置方式：将配置文件放置在 Isaac Sim 的 `isaacsim.sensors.rtx` 扩展目录下，并修改 `extension.toml`。

#### 3.1.2 场景文件 (`world/`)
- `HB_RemteExcavato_PistonDrive.usda` - 活塞驱动挖掘机场景
- `HB_RemteExcavator_AngleDrive.usda` - 角度驱动挖掘机场景
- `MyTerrain.usdc` - 地形场景
- `FinalTerrain/` - 最终地形资源

#### 3.1.3 物理仿真修正 (`warp_modification/`)
- **功能**: 修正 Warp 粒子模拟器的物理参数
- **文件**: `OgnParticlesSimulate.py`
- **修正内容**: 添加地面法线方向设置 (`normal=[0.0, 0.0, 1.0]`)，确保粒子与地面碰撞模拟正确。

**传感器输出话题**:

- `/front_camera` - 前相机
- `/front_surround_camera` - 前环视相机
- `/left_camera` - 左相机
- `/rear_camera` - 后相机
- `/right_camera` - 右相机

**控制输入话题**:
- `/pc2000_joint_command` - 关节控制命令 (JointState)

---

### 3.2 挖掘机侧控制软件 (Excavator-side Control Software)

**位置**: `controller/` 和 `webrtc_excavator/`

#### 3.2.1 图像处理软件 (ImgProvider)

**位置**: `ImgProvider/cpp_img_stitcher/`

**功能**:
- 接收多个相机数据流
- 进行图像拼接处理（支持多种拼接模式）
- 输出拼接后的图像到 `/stitched_image` 话题

**主要节点**:

##### a) multi_camera_stitcher_node - 多摄像头拼接节点

**功能**:
- 将6个摄像头的图像按照配置的布局拼接成一个大画面
- 支持部分摄像头输入（不需要全部6个摄像头都发布消息）
- 可配置的拼接布局（上下两行）
- 可配置的输出图像大小
- 输出 rgb8 格式图像

**输入话题**（所有输入必须是 rgb8 格式，1280x720）:
- `/boom_camera` (sensor_msgs/Image, rgb8, 1280x720) [可选]
- `/front_camera` (sensor_msgs/Image, rgb8, 1280x720) [可选]
- `/front_surround_camera` (sensor_msgs/Image, rgb8, 1280x720) [可选]
- `/left_camera` (sensor_msgs/Image, rgb8, 1280x720) [可选]
- `/rear_camera` (sensor_msgs/Image, rgb8, 1280x720) [可选]
- `/right_camera` (sensor_msgs/Image, rgb8, 1280x720) [可选]

**输出话题**:
- `/stitched_image` (sensor_msgs/Image, rgb8, 默认3840x1440)

**默认拼接布局**:
```
┌──────────┬──────────┬──────────┐
│          │          │          │
│   Left   │  Front   │  Right   │  <- 上行
│          │          │          │
├──────────┼──────────┼──────────┤
│          │  Front   │          │
│   Rear   │ Surround │   Boom   │  <- 下行
│          │          │          │
└──────────┴──────────┴──────────┘
```

**配置参数**:
- 摄像头话题：可配置各摄像头话题名称
- 输出话题：`output_topic` (默认: "/stitched_image")
- 布局配置：`top_row_layout`, `bottom_row_layout`
- 图像尺寸：`input_width`, `input_height`, `output_width`, `output_height`
- 输出频率：`output_rate` (默认: 30.0 Hz)
- 容错模式：`require_all_cameras` (默认: false，支持部分摄像头输入)

**启动方式**:
```bash
# 使用默认参数
ros2 launch cpp_img_stitcher multi_camera_stitcher.launch.py

# 使用配置文件
ros2 launch cpp_img_stitcher multi_camera_stitcher_with_config.launch.py
```

##### b) pip_camera_stitcher_node - 画中画摄像头拼接节点

**功能**:

- 创建画中画（PIP）布局
- `front_surround_camera` 作为主画面（全屏背景）
- 其他摄像头的小画面叠加在四个角上
- 自动适应输入图像方向（竖屏输入保持竖屏，横屏输入保持横屏）
- 支持部分摄像头输入

**输入话题**:
- `/front_surround_camera` (sensor_msgs/Image, rgb8, 1280x720) **主画面，必需，横屏**
- `/front_camera` (sensor_msgs/Image, rgb8, 1280x720) [可选，横屏]
- `/left_camera` (sensor_msgs/Image, rgb8, 720x1280) [可选，**竖屏**]
- `/rear_camera` (sensor_msgs/Image, rgb8, 1280x720) [可选，横屏]
- `/right_camera` (sensor_msgs/Image, rgb8, 720x1280) [可选，**竖屏**]

**输出话题**:
- `/stitched_image` (sensor_msgs/Image, rgb8, 默认1280x720)

**画面布局**:
```
┌─────────────────────────────────────┐
│L┐              ┌R                   │
│e│              │i                   │
│f│              │g                   │
│t│              │h                   │
│ │              │t                   │
│                                      │
│      主画面 (front_surround)         │
│         横屏 1280x720                │
│                                      │
│Front───┐        ┌───Rear            │
│────────┘        └────────           │
└─────────────────────────────────────┘
```

**性能特点**:
- **低延迟**：主画面到达时立即处理，延迟<5ms
- **零拷贝**：使用共享指针避免图像拷贝
- **事件驱动**：输出帧率自动匹配主画面帧率
- **优化插值**：使用 INTER_LINEAR 快速缩放
- **防积压**：智能跳帧避免延迟累积

**配置参数**:
- 摄像头话题：可配置各摄像头话题名称
- 输出配置：`output_topic`, `output_width`, `output_height`
- 小画面配置：`left_scale`, `right_scale`, `front_scale`, `rear_scale` (默认: 0.15)
- 位置参数：`left_x`, `left_y`, `right_x`, `right_y`, `front_x`, `front_y`, `rear_x`, `rear_y` (-1表示自动贴边)

**启动方式**:
```bash
# 使用默认参数
ros2 launch cpp_img_stitcher pip_camera_stitcher.launch.py

# 使用配置文件
ros2 launch cpp_img_stitcher pip_camera_stitcher_with_config.launch.py
```

##### c) stitcher_node - 双摄像头拼接节点

**功能**:
- 将两个摄像头的图像拼接成一个宽视场图像
- 输出 I420 格式图像

**输入话题**:
- `/camera` (sensor_msgs/Image, rgb8, 1280x720)
- `/camera_left` (sensor_msgs/Image, rgb8, 1280x720)

**输出话题**:
- `/camera_front_wide` (sensor_msgs/Image, I420, 1920x1080)

##### d) i420_to_rgb_total_node - 图像格式转换节点

**功能**:
- 将 I420 格式的图像转换为 RGB 格式

**输入话题**:
- 输入话题 (sensor_msgs/Image, I420)

**输出话题**:
- 输出话题 (sensor_msgs/Image, RGB8)

**构建和安装**:
```bash
mkdir -p ~/Img_ws/src
cp ImgProvider/ ~/Img_ws/src
cd ~/Img_ws
colcon build --symlink-install
source install/setup.bash
```

#### 3.2.2 控制软件

**位置**: `controller/src/keyboard_teleop/`

**主要文件**:
- `teleop_prismatic_publisher.py` - 主控制节点（位置控制模式）
- `teleop_advanced_control.py` - 高级控制节点（带校准、死区、限幅等功能）

##### a) teleop_prismatic_publisher.py - 基础控制节点

**功能**:
- 接收远程控制指令 (`/controls/teleop`)
- 将控制指令转换为关节命令（位置控制模式）
- 发布关节控制命令到仿真环境

**控制模式**: **位置控制**（直接映射到目标位置）

**控制参数映射**:

- `bucket` (铲斗): -1.0 ~ 1.0 → `bucket_linear`: -3.0m ~ 3.0m
- `boom` (动臂): -1.0 ~ 1.0 → `arm_linear`: -3.0m ~ 3.0m（注意：前端boom映射到ROS2的arm_linear）
- `stick` (斗杆): -1.0 ~ 1.0 → `boom_linear`: -3.0m ~ 3.0m（注意：前端stick映射到ROS2的boom_linear）
- `swing` (回转): -1.0 ~ 1.0 → `body_rotate`: -π ~ π (弧度，约±180°)
- `track_left` / `track_right` (履带): -1.0 ~ 1.0 → 速度控制

**映射公式**:
```python
bucket_pos = -bucket_input * 3.0      # bucket → bucket_linear
arm_pos = -boom_input * 3.0           # boom → arm_linear (大臂)
boom_pos = -stick_input * 3.0         # stick → boom_linear (小臂)
body_yaw = swing_input * math.pi      # swing → body_rotate
```

**输入话题**:
- `/controls/teleop` (std_msgs/String) - JSON 格式控制指令

**输出话题**:
- `/pc2000_joint_command` (sensor_msgs/JointState) - 关节控制命令

**特点**:
- 事件驱动：仅在收到新消息时发布（无定时器）
- 位置控制：输入值直接映射到目标位置
- 与前端映射逻辑完全一致

##### b) teleop_advanced_control.py - 高级控制节点

**功能**:
在基础控制节点的基础上，添加了以下高级控制功能：

1. **校准和归一化** - 处理手柄中位偏移
2. **死区（Deadzone）** - 中间 ±3%～±20% 不响应，防止轻微抖动
3. **限幅和斜坡（Rate Limit）** - 限制指令变化速率，每20ms最大变化5%
4. **非线性响应曲线** - 低段细、末端陡，便于微动操作

**输入处理流程**:
```
原始输入 [-1, 1]
    ↓
1. 校准（减去中位偏移）
    ↓
2. 死区处理（死区内→0，死区外→线性映射）
    ↓
3. 非线性响应曲线（幂函数变换）
    ↓
4. 限幅和斜坡（限制变化速率）
    ↓
最终输出 [-1, 1]
```

**配置参数**:
- `deadzone_default`: 默认死区（默认: 0.10 = 10%，范围: 3%～20%）
- `rate_limit_percent`: 每周期最大变化百分比（默认: 0.05 = 5%）
- `control_period_ms`: 控制周期（默认: 20ms，50Hz）
- `response_curve_exponent`: 响应曲线指数（默认: 2.0，平方曲线）
- `enable_calibration`: 是否启用校准（默认: true）
- `calibration_samples`: 校准采样次数（默认: 100）

**服务接口**:
- `/teleop_advanced/start_calibration` (std_srvs/Trigger) - 启动校准
- `/teleop_advanced/stop_calibration` (std_srvs/Trigger) - 停止校准并计算偏移

**订阅话题**:
- `/controls/teleop` (std_msgs/String) - 遥操作控制输入
- `/joint_states` (sensor_msgs/JointState) - 关节状态反馈（可选）

**发布话题**:
- `/pc2000_joint_command` (sensor_msgs/JointState) - 关节命令输出

**使用场景**:
- **精细操作场景**（如精细土方）：增大死区、减小限幅、提高响应曲线指数
- **快速操作场景**：减小死区、增大限幅、降低响应曲线指数

**控制指令格式**:
```json
{
  "bucket": 0.5,
  "stick": 0.0,
  "boom": 0.0,
  "swing": 0.0,
  "device_type": "excavator",
  "timestamp": 1699363200000
}
```

**完整控制指令格式**（支持履带控制）:
```json
{
  "rotation": 0.0,
  "brake": 0.0,
  "throttle": 0.0,
  "gear": "N",
  "boom": 0.0,
  "bucket": 0.0,
  "left_track": 0.0,
  "right_track": 0.0,
  "swing": 0.0,
  "stick": 0.0,
  "device_type": "excavator",
  "timestamp": 1698765432000
}
```

#### 3.2.3 实时通信客户端

**位置**: `webrtc_excavator/`

**架构**: 共享内存 + 进程隔离方案 (v3.0)

**核心组件**:

##### a) ROS2 到共享内存桥接 (`scripts/shm_solution/ros_to_shm.py`)

**功能**:
- 订阅 `/stitched_image` 话题（sensor_msgs/Image, rgb8）
- 将原始 RGB 图像数据写入共享内存 (`/dev/shm/isaac_rgb_buffer`)
- 支持动态分辨率适配

**特点**:
- 纯 ROS2 节点（仅使用 `rclpy`，避免库冲突）
- 零拷贝数据传输
- 纳秒级延迟
- 自动处理分辨率变化

**共享内存格式**（32 字节头 + 图像数据）:
```
Offset | Size | Field
-------|------|------------------
0      | 8    | timestamp_ns (uint64)
8      | 4    | width (uint32)
12     | 4    | height (uint32)
16     | 8    | frame_counter (uint64)
24     | 8    | reserved (padding)
32     | N    | RGB image data (width * height * 3 bytes)
```

**QoS 配置**:
- `depth=1`: 只保留最新的一帧
- `reliability=BEST_EFFORT`: 允许丢帧，优先低延迟
- `history=KEEP_LAST`: 不保留历史数据

##### b) GStreamer 硬件编码器 (`scripts/shm_solution/shm_to_stdout.py`)

**功能**:
- 从共享内存读取图像数据
- 使用 NVIDIA 硬件编码器 (`nvh264enc`) 进行 H.264 编码
- 通过 `fdsink fd=1` 将 H.264 码流输出到 stdout，供 Go 子进程直接读取

**特点**:
- 纯 GStreamer 程序（仅使用 `gi`，避免与 ROS2 库冲突）
- 完全进程隔离，最低延迟
- Go 端无需改动读取逻辑

**GStreamer 管线**:
```
appsrc 
  → video/x-raw,format=RGB 
  → videoconvert 
  → video/x-raw,format=I420 
  → nvh264enc (硬件编码)
  → h264parse 
  → fdsink fd=1 (输出到 stdout)
```

**编码参数**:
- 编码器: `nvh264enc` (Jetson) 或 `x264enc` (x86)
- 输出格式: H.264 byte-stream
- 帧率: 动态适配（默认 30fps）
- 比特率: 可配置（默认 4000 kbps）

**替代方案**: `shm_to_h264_publisher.py`（可选/调试）
- GStreamer + ROS2 混合节点：从共享内存读取、硬编、并发布到 `/camera/image_h264`
- 适用于需要在 ROS 可视化工具中直接消费 H.264 的场景
- 注意：此脚本与 `rclpy` 同进程使用 GStreamer，可能在某些环境下引发库冲突；推荐生产环境使用 `shm_to_stdout.py`

##### c) Go WebRTC 客户端 (`cmd/excavator/main.go`)

**功能**:
- 读取 GStreamer 输出的 H.264 视频流
- 建立 WebRTC PeerConnection
- 通过信令服务器与远程控制端建立连接
- 接收控制指令并转发到 ROS2

**关键流程**:
1. 连接信令服务器 (WebSocket)
2. 注册为 "excavator" 身份
3. 等待控制端连接
4. 处理 WebRTC 信令 (Offer/Answer/ICE)
5. 建立视频轨道和控制数据通道
6. 转发视频流和控制指令

##### d) ROS2 控制转发器 (`scripts/shm_solution/ros_control_stdin.py`)

**功能**:
- 从 stdin 读取 JSON 格式控制指令
- 验证 JSON 格式
- 发布到 ROS2 话题 `/controls/teleop` (std_msgs/String)

**特点**:
- 纯 ROS2 节点（仅使用 `rclpy`）
- **自动启动**：Go 客户端会自动启动此脚本
- 支持从 stdin 管道读取控制指令

**数据格式**: JSON 字符串，每行一条指令

**手动测试**:
```bash
echo '{"bucket":0.5,"device_type":"excavator","timestamp":1234567890}' | \
  python3 ros_control_stdin.py --ros-args -p control_topic:=/controls/teleop
```

---

### 3.3 云服务器端软件 (Cloud Server-side Software)

**位置**: 云服务器（独立部署）

#### 3.3.1 信令服务器 (Signaling Server)

**功能**:
- 提供 WebSocket 信令服务
- 管理 WebRTC 连接建立
- 转发 Offer/Answer/ICE 候选

**端口**: 8090 (WebSocket)

**协议**:
- WebSocket (ws:// 或 wss://)
- JSON 消息格式

**消息类型**:
- `register` - 客户端注册
- `offer` - WebRTC Offer
- `answer` - WebRTC Answer
- `candidate` - ICE 候选

**部署方式**:
- 可部署到云服务器（阿里云、腾讯云、AWS 等，目前部署在交大云）
- 支持 systemd 服务管理
- 支持开机自启动

**部署文档**: `webrtc_excavator/CLOUD_DEPLOYMENT.md`

#### 3.3.2 云端挖机管理系统

**功能**:
- 挖掘机设备管理
- 连接状态监控
- 日志记录和分析

**状态**: 规划中

---

### 3.4 远程遥控侧软硬件 (Remote Teleoperation-side)

**位置**: `exca-teleop/`

#### 3.4.1 远程控制 Web 应用 (exca-teleop)

**技术栈**:
- **框架**: Next.js 16.0 (React 19.2)
- **UI 组件**: Radix UI + Tailwind CSS
- **WebRTC**: 浏览器原生 WebRTC API
- **语言**: TypeScript

**主要功能**:

##### a) 用户认证系统
- 用户登录界面
- 会话管理
- 设备选择（挖掘机名称配置）

##### b) WebRTC 视频接收
- 通过 `useWebRTC` Hook 管理 WebRTC 连接
- 接收并显示挖掘机视频流
- 连接状态监控（连接中/已连接/已断开）
- 延迟监控（ping 检测）

##### c) 传感器数据展示
- **液压压力**: 实时显示液压系统压力（0-100）
- **臂角度**: 显示动臂角度（0-90°）
- **铲斗角度**: 显示铲斗角度（0-60°）
- **引擎温度**: 显示引擎温度，支持高温预警（>95°C）

##### d) 连接管理
- 信令服务器配置
- 连接日志记录
- 连接质量评估（优秀/良好/较差/严重）
- 自动重连机制

##### e) 设置面板
- 用户信息管理
- 挖掘机设备选择
- 信令服务器地址配置
- 连接日志查看

**核心组件**:

- `app/page.tsx` - 主控制界面
- `hooks/useWebRTC.ts` - WebRTC 连接管理 Hook
- `components/login-screen.tsx` - 登录界面
- `components/settings-panel.tsx` - 设置面板
- `components/hydraulic-gauge.tsx` - 液压压力表盘
- `components/status-indicator.tsx` - 连接状态指示器
- `components/connection-log.tsx` - 连接日志组件

**WebRTC 连接流程**:

1. 连接信令服务器 (WebSocket)
2. 注册为 "controller" 身份
3. 创建 RTCPeerConnection
4. 创建 Offer 并发送
5. 接收 Answer 和 ICE 候选
6. 建立视频轨道接收
7. 定期发送 ping 检测延迟

**输入话题**:
- 信令服务器地址（可配置，默认: `ws://111.186.56.118:8090/ws`）

**输出**:
- 视频流显示（HTML5 video 元素）
- 传感器数据可视化（HUD 界面）
- 连接状态和日志

**部署方式**:
- 开发模式: `npm run dev` (localhost:3000)
- 生产模式: `npm run build && npm start`
- 可部署到 Vercel、Netlify 等平台

#### 3.4.2 远程遥控驾驶硬件系统

**实现**: exca-teleop Web 应用 + 输入设备

**功能**:
- 接收物理控制信号（摇杆、踏板输入）
- 转换为控制指令
- 通过 WebRTC DataChannel 发送

**输入设备**:
- 游戏手柄/摇杆
- 键盘
- 触摸屏

#### 3.4.3 遥控驾驶辅助系统

**功能**:
- 路径规划辅助
- 碰撞检测预警
- 操作建议

**状态**: 规划中

---

## 4. 硬件架构

### 4.1 仿真电脑 (Simulation Computer)

**功能**:
- 运行 Isaac Sim 仿真环境
- 生成传感器数据
- 执行物理仿真

**要求**:
- 支持 NVIDIA GPU（推荐）
- Ubuntu 22.04
- ROS2 Humble

**连接**:
- 通过网线连接到 Orin 域控制器

### 4.2 Orin 域控制器 (Orin Domain Controller)

**功能**:
- 运行挖掘机侧控制软件
- 运行 WebRTC 客户端
- 处理图像编码

**硬件**:
- NVIDIA Jetson Orin / Xavier
- 支持硬件视频编码

**连接**:
- 网线连接到仿真电脑
- 网络连接到云服务器

### 4.3 云服务器 (Cloud Server)

**功能**:
- 运行信令服务器
- 提供公网访问入口

**要求**:
- 公网 IP
- Ubuntu 22.04
- 最低配置: 1 核 CPU, 1GB 内存
- 推荐配置: 2 核 CPU, 2GB 内存

**端口**:
- 8090: WebSocket 信令服务器
- 8080: Web 控制页面（可选）

### 4.4 遥控侧硬件 (Remote Control-side Hardware)

**设备**:
- 摇杆/游戏手柄
- 踏板
- 显示器
- 计算机/平板

**连接**:
- 通过互联网连接到云服务器

---

## 5. 通信协议

### 5.1 ROS2 话题

#### 5.1.1 视频话题

| 话题名称 | 消息类型 | 说明 |
|---------|---------|------|
| `/front_camera` | sensor_msgs/Image | 前鱼眼相机 |
| `/front_surround_camera` | sensor_msgs/Image | 前环视相机 |
| `/left_camera` | sensor_msgs/Image | 左鱼眼相机 |
| `/rear_camera` | sensor_msgs/Image | 后鱼眼相机 |
| `/right_camera` | sensor_msgs/Image | 右鱼眼相机 |
| `/stitched_image` | sensor_msgs/Image (rgb8) | 拼接后的图像 |

#### 5.1.2 控制话题

| 话题名称 | 消息类型 | 说明 |
|---------|---------|------|
| `/controls/teleop` | std_msgs/String | 远程控制指令（JSON） |
| `/pc2000_joint_command` | sensor_msgs/JointState | 关节控制命令 |

### 5.2 WebRTC 信令协议

**信令服务器地址**: `ws://<云服务器IP>:8090/ws`

**消息格式**:
```json
{
  "type": "register|offer|answer|candidate",
  "from": "excavator|controller",
  "to": "excavator|controller",
  "payload": { ... }
}
```

### 5.3 控制指令协议

**格式**: JSON 字符串（通过 std_msgs/String 传输）

**基础格式**:
```json
{
  "bucket": 0.5,      // 铲斗控制: -1.0 ~ 1.0
  "stick": 0.0,       // 斗杆控制: -1.0 ~ 1.0
  "boom": 0.0,        // 动臂控制: -1.0 ~ 1.0
  "swing": 0.0,       // 回转控制: -1.0 ~ 1.0
  "device_type": "excavator",
  "timestamp": 1699363200000
}
```

**完整格式**（支持履带控制）:
```json
{
  "rotation": 0.0,        // 旋转控制: -1.0 ~ 1.0
  "brake": 0.0,          // 刹车: 0.0 ~ 1.0
  "throttle": 0.0,        // 油门: 0.0 ~ 1.0
  "gear": "N",            // 档位: "N", "D", "R"，"P"
  "boom": 0.0,            // 动臂控制: -1.0 ~ 1.0
  "bucket": 0.0,          // 铲斗控制: -1.0 ~ 1.0
  "left_track": 0.0,      // 左履带: -1.0 ~ 1.0
  "right_track": 0.0,     // 右履带: -1.0 ~ 1.0
  "swing": 0.0,           // 回转控制: -1.0 ~ 1.0
  "stick": 0.0,           // 斗杆控制: -1.0 ~ 1.0
  "device_type": "excavator",
  "timestamp": 1698765432000
}
```

**字段说明**:
- `bucket`: 铲斗控制，-1.0（收回）到 1.0（翻出）
- `boom`: 动臂控制，-1.0（下降）到 1.0（提升），映射到 ROS2 的 `arm_linear`
- `stick`: 斗杆控制，-1.0（收回）到 1.0（伸出），映射到 ROS2 的 `boom_linear`
- `swing`: 回转控制，-1.0（左旋）到 1.0（右旋），映射到 ROS2 的 `body_rotate`
- `left_track` / `right_track`: 履带速度控制，-1.0（后退）到 1.0（前进）
- `device_type`: 设备类型，固定为 "excavator"
- `timestamp`: 时间戳（毫秒）

**ROS2 话题格式**:
- **话题名称**: `/controls/teleop`
- **消息类型**: `std_msgs/String`
- **消息内容**: JSON 字符串（与前端发送的格式相同）
- **QoS**: `depth=1, BEST_EFFORT, KEEP_LAST`

---

## 6. 数据流详细说明

### 6.1 视频流处理流程

1. **传感器数据采集**
   - Isaac Sim 生成多路相机数据
   - 发布到 ROS2 话题

2. **图像拼接**
   - ImgProvider 节点（multi_camera_stitcher_node 或 pip_camera_stitcher_node）接收多路图像
   - 进行图像拼接处理（网格拼接或画中画模式）
   - 输出到 `/stitched_image` 话题（rgb8 格式）

3. **共享内存写入**
   - `ros_to_shm.py` 订阅 `/stitched_image`
   - 将图像数据写入 `/dev/shm/isaac_rgb_buffer`
   - 包含时间戳和分辨率信息

4. **硬件编码**
   - `shm_to_stdout.py` 从共享内存读取
   - GStreamer 使用 `nvh264enc` 编码
   - 输出 H.264 流到 stdout

5. **WebRTC 传输**
   - Go 客户端读取 H.264 流
   - 封装为 WebRTC 视频轨道
   - 通过 PeerConnection 发送

6. **远程显示**
   - exca-teleop Web 应用接收视频流
   - 浏览器自动解码
   - 在 HTML5 video 元素中显示
   - 支持全屏显示和 HUD 叠加

### 6.2 控制流处理流程

1. **用户输入**
   - exca-teleop Web 应用接收用户输入（摇杆/键盘/游戏手柄）
   - 转换为控制指令 JSON
   - 通过 WebRTC DataChannel 发送

2. **WebRTC 传输**
   - 通过 DataChannel 发送控制指令（JSON 字符串）
   - 经过信令服务器转发（仅信令，数据直连）
   - Go 客户端接收控制指令

3. **ROS2 转发**
   - Go 客户端接收控制指令
   - 写入到 `ros_control_stdin.py` 的 stdin（管道）
   - Python 脚本验证 JSON 格式并发布到 `/controls/teleop` 话题

4. **控制处理**
   - `teleop_prismatic_publisher.py` 或 `teleop_advanced_control.py` 订阅控制话题
   - 解析 JSON 指令
   - 转换为关节命令（位置控制模式）
   - 高级控制节点会进行：校准、死区处理、非线性响应曲线、限幅和斜坡

5. **执行控制**
   - 发布到 `/pc2000_joint_command` 话题（sensor_msgs/JointState）
   - Isaac Sim 接收并执行关节控制命令

---

## 7. 性能指标

### 7.1 延迟指标

- **端到端视频延迟**: ~20ms
- **控制指令延迟**: ~10-30ms
- **共享内存传输**: 纳秒级

### 7.2 编码性能

- **编码器**: NVIDIA nvh264enc (硬件加速)
- **编码帧率**: 30fps (可配置)
- **分辨率**: 动态适配（默认 1280x720）
- **CPU 占用**: 低（硬件编码）

### 7.3 网络要求

- **信令服务器带宽**: 1-3 Mbps
- **视频流带宽**: 取决于分辨率和编码参数
- **控制流带宽**: < 1 Mbps

---

## 8. 部署架构

### 8.1 本地测试架构

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│  浏览器      │────►│ 信令服务器    │◄────│  Orin 设备   │
│ 控制端       │     │ (localhost)  │     │ 挖掘机程序   │
└─────────────┘     └──────────────┘     └─────────────┘
    同一局域网（192.168.3.x）
```

### 8.2 云部署架构（推荐）

```
                    ┌──────────────────┐
                    │   云服务器        │
                    │  (公网 IP)       │
                    │  信令服务器       │
                    │  8090 端口        │
                    └────────┬─────────┘
                             │
            ┌────────────────┼────────────────┐
            │                │                │
            ▼                ▼                ▼
    ┌─────────────┐  ┌──────────────┐  ┌─────────────┐
    │  控制端 A    │  │  控制端 B     │  │  Orin 设备   │
    │ (任何位置)   │  │  (任何位置)   │  │  (工地现场)  │
    └─────────────┘  └──────────────┘  └─────────────┘
        家里/办公室      移动端            4G/5G/WiFi
```

---

## 9. 关键技术特性

### 9.1 进程隔离架构

**优势**:
- 避免 ROS2 和 GStreamer 库冲突
- 提高系统稳定性
- 独立进程崩溃不影响整体系统

**实现**:
- ROS2 节点运行在独立进程
- GStreamer 编码器运行在独立进程
- Go 主进程管理子进程生命周期

### 9.2 共享内存通信

**优势**:
- 零拷贝数据传输
- 纳秒级延迟
- 高吞吐量

**实现**:
- 使用 `/dev/shm` 共享内存
- 原子写入操作
- 支持动态分辨率

### 9.3 硬件加速编码

**优势**:
- 低 CPU 占用
- 高编码效率
- 实时性能

**实现**:
- Jetson 平台: `nvh264enc`
- x86 平台: `x264enc` 或 `nvh264enc` (NVIDIA GPU)

### 9.4 WebRTC 实时通信

**优势**:
- 低延迟
- 自适应码率
- NAT 穿透

**实现**:
- Pion WebRTC 库
- 信令服务器中继
- ICE 候选交换

---

## 10. 文件结构

```
RemoteExcavator/
├── simulator/                    # 仿真器
│   ├── sensor_simulation/        # 传感器模拟
│   ├── warp_modification/        # Warp 粒子模拟修改
│   └── world/                    # 场景文件
├── controller/                   # 控制软件
│   └── src/keyboard_teleop/      # 键盘/手柄控制
│       ├── teleop_prismatic_publisher.py  # 主控制节点
│       └── ...
└── webrtc_excavator/             # WebRTC 通信
    ├── cmd/excavator/            # Go 主程序
    │   └── main.go
    ├── pkg/ros2/                 # ROS2 客户端库
    ├── scripts/                  # 脚本文件
    │   ├── shm_solution/         # 共享内存方案
    │   │   ├── ros_to_shm.py     # ROS2 → 共享内存
    │   │   ├── shm_to_stdout.py  # 共享内存 → H.264
    │   │   └── ros_control_stdin.py  # 控制转发
    │   ├── start-excavator-bridge.sh
    │   └── start-signaling.sh
    ├── README.md
    └── CLOUD_DEPLOYMENT.md

ImgProvider/                      # 图像处理节点
└── cpp_img_stitcher/             # C++ 图像拼接包
    ├── src/                      # 源代码
    │   ├── multi_camera_stitcher_node.cpp    # 多摄像头拼接节点
    │   ├── pip_camera_stitcher_node.cpp      # 画中画拼接节点
    │   ├── stitcher_node.cpp                 # 双摄像头拼接节点
    │   └── i420_to_rgb_total_node.cpp        # 格式转换节点
    ├── launch/                   # Launch 文件
    │   ├── multi_camera_stitcher.launch.py
    │   ├── multi_camera_stitcher_with_config.launch.py
    │   ├── pip_camera_stitcher.launch.py
    │   ├── pip_camera_stitcher_with_config.launch.py
    │   └── stitcher.launch.py
    ├── config/                   # 配置文件
    │   ├── multi_camera_stitcher_params.yaml
    │   └── pip_camera_stitcher_params.yaml
    ├── CMakeLists.txt
    ├── package.xml
    ├── README.md
    ├── USAGE_ZH.md
    └── PIP_USAGE_ZH.md

exca-teleop/                      # 远程控制 Web 应用
├── app/                          # Next.js App Router
│   ├── page.tsx                  # 主控制界面
│   ├── layout.tsx                # 布局组件
│   └── globals.css               # 全局样式
├── components/                    # React 组件
│   ├── login-screen.tsx          # 登录界面
│   ├── settings-panel.tsx        # 设置面板
│   ├── hydraulic-gauge.tsx       # 液压压力表盘
│   ├── status-indicator.tsx      # 连接状态指示器
│   ├── connection-log.tsx        # 连接日志
│   └── ui/                       # UI 基础组件
│       ├── button.tsx
│       ├── input.tsx
│       ├── label.tsx
│       └── ...
├── hooks/                        # React Hooks
│   └── useWebRTC.ts              # WebRTC 连接管理
├── lib/                          # 工具库
│   └── utils.ts                  # 工具函数
├── public/                       # 静态资源
├── package.json                  # 项目依赖
├── tsconfig.json                 # TypeScript 配置
└── README.md                     # 项目说明
```

---

## 11. 启动流程

### 11.1 挖掘机端启动

1. **启动 Isaac Sim 仿真**
   ```bash
   # 在仿真电脑上
   ./isaac-sim.sh
   ```

2. **启动图像处理节点**（ImgProvider）
   ```bash
   # 方式1：使用多摄像头拼接节点（推荐，网格布局）
   ros2 launch cpp_img_stitcher multi_camera_stitcher.launch.py
   
   # 方式2：使用画中画拼接节点（PIP模式）
   ros2 launch cpp_img_stitcher pip_camera_stitcher.launch.py
   
   # 方式3：使用配置文件启动
   ros2 launch cpp_img_stitcher multi_camera_stitcher_with_config.launch.py
   ```

3. **启动 ROS2 到共享内存桥接**
   ```bash
   cd webrtc_excavator/scripts/shm_solution
   ./start_ros_to_shm.sh
   ```

4. **启动 WebRTC 桥接程序**
   ```bash
   cd webrtc_excavator/scripts
   ./start-excavator-bridge.sh
   ```

### 11.2 云服务器启动

1. **启动信令服务器**
   
   ```bash
   cd webrtc_excavator/scripts
   ./start-signaling.sh
   ```
```
   
   或使用 systemd 服务:
   ```bash
   sudo systemctl start signaling
```

### 11.3 控制端连接

1. **启动 exca-teleop Web 应用**
   ```bash
   cd exca-teleop
   npm install
   npm run dev
   ```
   或生产模式:
   ```bash
   npm run build
   npm start
   ```

2. **打开浏览器**
   - 访问 `http://localhost:3000` (开发模式)
   - 或访问部署后的地址（生产环境）

3. **登录和配置**
   - 输入用户名和密码登录
   - 在设置面板中配置挖掘机名称
   - 配置信令服务器地址（如需要）

4. **建立连接**
   - 应用自动连接信令服务器
   - 建立 WebRTC 连接
   - 开始接收视频流和传感器数据

---

## 12. 故障排查

### 12.1 视频流问题

**问题**: 无法看到视频
- 检查 `/stitched_image` 话题是否有数据
- 检查共享内存是否创建成功
- 检查 GStreamer 编码器是否正常运行
- 检查 WebRTC 连接状态

### 12.2 控制问题

**问题**: 控制指令无响应
- 检查 `/controls/teleop` 话题是否有消息
- 检查控制节点是否运行
- 检查 WebRTC DataChannel 是否建立
- 检查 JSON 格式是否正确

### 12.3 连接问题

**问题**: 无法连接到信令服务器
- 检查云服务器防火墙设置
- 检查端口 8090 是否开放
- 检查信令服务器是否运行
- 检查网络连接

### 12.4 可视化调试

**工具**: Foxglove Studio (基于 Web 的可视化软件)

**功能**:
- 实时监控 ROS2 话题数据
- 可视化查看相机图像流
- 监控关节状态和控制指令
- 调试雷达点云数据

**使用方式**:
- 启动 `foxglove_bridge` (需单独安装)
- 通过浏览器访问 Foxglove Studio
- 连接到 ROS2 系统进行数据监控

---

## 13. 未来规划

### 13.1 功能增强

- [ ] 云端挖机管理系统：多挖机实时状态管理、挖机数据上传
- [ ] 遥控驾驶辅助系统：倾覆主动安全、行人主动安全
- [ ] 多挖掘机管理
- [ ] 数据记录和回放
- [ ] 铲斗、斗杆、动臂倾角检测
- [ ] 铲斗落点预测功能
- [ ] 铲斗深度感知功能

### 13.2 性能优化

- [ ] 自适应码率控制
- [ ] 多路视频流支持
- [ ] 更高效的编码参数
- [ ] 网络拥塞控制

### 13.3 安全性

- [ ] HTTPS/WSS 支持
- [ ] 身份认证
- [ ] 数据加密
- [ ] 访问控制

---

## 14. 参考资料

### 14.1 项目文档

- `webrtc_excavator/README.md` - WebRTC 桥接详细文档
- `webrtc_excavator/CLOUD_DEPLOYMENT.md` - 云服务器部署指南
- `controller/src/keyboard_teleop/QUICKSTART.md` - 控制软件快速开始
- `simulator/readme.md` - 仿真器说明
- `exca-teleop/README.md` - 远程控制 Web 应用说明
- `ImgProvider/cpp_img_stitcher/README.md` - 图像处理节点说明
- `ImgProvider/cpp_img_stitcher/USAGE_ZH.md` - 多摄像头拼接节点使用说明
- `ImgProvider/cpp_img_stitcher/PIP_USAGE_ZH.md` - 画中画拼接节点使用说明

### 14.2 技术文档

- [Isaac Sim 文档](https://docs.omniverse.nvidia.com/app_isaacsim/app_isaacsim/overview.html)
- [ROS2 文档](https://docs.ros.org/en/humble/)
- [WebRTC 文档](https://webrtc.org/)
- [Pion WebRTC](https://github.com/pion/webrtc)

---

## 15. 版本信息

- **文档版本**: 1.4
- **系统版本**: v3.0 (Shared Memory Solution)
- **最后更新**: 2025-11-19
- **更新内容**: 
  - 添加 ImgProvider 图像处理节点详细文档
  - 修正数据流方向说明
  - 完善图像处理软件章节
  - 添加高级控制节点详细说明
  - 更新控制模式说明（位置控制）
  - 完善共享内存方案技术细节
  - 更新控制指令协议格式
  - 补充仿真器雷达配置和物理修正说明
  - 添加 Foxglove 可视化调试说明

---

**文档维护**: 请根据系统更新及时更新本文档。

